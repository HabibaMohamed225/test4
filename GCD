#include <iostream>
#include<cmath>
using namespace std;
void GCD(int n1,int n2){
    int j=0,target=1;
    for(int i=1;i<=max(n1,n2);i++){
        if((n1%i==0)&&(n2%i==0)){
            target=i;

        }
    }
    cout<<target;
}
int main(){
    int num1,num2;
    cin>>num1>>num2;
    GCD(num1,num2);
    return 0;
}
